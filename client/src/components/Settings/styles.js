import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles((theme) => ({
  menuBtn: {
    position: 'absolute',
    right: 0,
    top: 0,
  },
  menuBtnIcon: {},
  renewIntervalInput: { width: '3em' },
}))

export default useStyles
